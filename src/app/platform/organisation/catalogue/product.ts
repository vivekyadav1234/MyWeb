export class Product {
   constructor(
     public id ?: number,
     public name ?: string,
     public description ?: string,
     public section_id ?: Number,
     public price ?: string,
     public attachment_file ?: any
   ){ }
}
