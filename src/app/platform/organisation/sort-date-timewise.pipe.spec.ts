import { SortDateTimewisePipe } from './sort-date-timewise.pipe';

describe('SortDateTimewisePipe', () => {
  it('create an instance', () => {
    const pipe = new SortDateTimewisePipe();
    expect(pipe).toBeTruthy();
  });
});
