<!DOCTYPE html>
<html lang="en">
<head>
  <title>Email</title>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  <link rel="stylesheet" type="text/css" href="style.css">
</head>
<body>

  <!-- Nav -->
    <%= render("header") %>
  <!-- Nav -->

  <div class="container">
    <div class="row">
      <div class="col-12">
        <h5 class="mt-5">Hello </h5>
        <div>
          QC <%= @qcStatus.humanize.titleize %> for Lead Id: <%= @lead.id%>
          <p>Purchase Order<%= @purchase_order.reference_no %></p>
          <p>Project: <%= @project.name%></p>
          <p>Lead id: <%= @lead.id %></p>
          <p>CM Name: <%= @lead.assigned_cm&.name%> </p>
          <p>Desigenr: <%= @project.assigned_designer&.name %></p>
          <p>QC Date: <%= @qc.qc_date&.strftime("%d-%b-%Y")%></p>
          <p>QC Remarks: <%= @qc.remarks%> </p>
        </div>
      </div>
      <div class="col-12">
          <div>
            <table border="1" style="max-width:75%;">
                <thead class="thead-dark">
                  <tr>
                      <th>PO No.</th>
                      <th>Sli</th>
                      <th>Quantity</th>
                      <th>Shipping Address</th>
                      <th>Vendor Name</th>
                  </tr>
                </thead>

                <tr>
                  <td><%= @purchase_order.reference_no%></td>
                  <td><%= @job_element.element_name%></td>
                  <td><%= @quantity%></td>
                  <td><%= @purchase_order.shipping_address%></td>
                  <td><%= @purchase_order.vendor.name%></td>
                </tr>
            </table>
          </div>
      </div>
    </div>
  </div>

  <%= render("footer") %>

</body>
<style>
  table, th, td
  {
    border: 1px solid black;
  }
</style>
</html>
